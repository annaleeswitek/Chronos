// TODO

// cart routes -- inside /login route; deal w/ unauthenticated users separately?
// cart middleware => similar to passport middleware, it adds shtuff onto the `req` body (`req.cart`) AND it will set `req.session.cartId = order.id`
// create the actual constructor Cart function w/ all its properties and methods


// REQUIREMENTS
// -- hold all prods that user puts in it
// -- persist from session to session
// -- needs to be an object
// -- needs to reside on user's session


// req.session.cart =

// req.cart = {
//   products: []
// }


